{
  "name": "@lens-and-sync/shared-utils",
  "version": "0.0.1",
  "private": true,
  "description": "Generic helpers (retry, validation, etc.) shared across apps.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lens-and-sync/shared-logger": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
