{
  "name": "@lens-and-sync/shared-types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types/interfaces across apps.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
