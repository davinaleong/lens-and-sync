// TODO: image intake (multipart/base64), file-type validation via magic bytes, size/dimension limits.
// Uploads go to Google Cloud Storage via pre-signed URLs (@google-cloud/storage), random UUID object keys.
export {};
